#### JAVA

### 1. 자바의 Method 정리

#### 1) String
```java
String str = "abcde"; // 초기화

str.length() // 
str.isEmpty() // 

str.charAt(2) // 
str.indexOf("c") // 
str.lastIndexOf("c") // 

str.substring(2, 4) // 
str.substring(3) // 

str.replace('b', 'k') // 

str.equals("abcde") // 
str.contains("bc") //

str.split(" ") // 
str.split() // 

str.trim() // 

str.toLowerCase() // 
str.toUpperCase() //

str.compareTo("abcdd") //

Integer.parseInt("300") // 
Integer.toString(300) // 
```
